/**
 * Pagination limit for GET /api/leads and “Get socials” (processes visible rows on the current page).
 * Change this single value to tune table density vs. batch duration.
 */
export const LEADS_PAGE_SIZE = 6;
